const any = {
  empty: 'Cannot be empty.'
}

const fallback = 'An error occured.'

const http = {
  unauthorized: 'Token has expired.'
}

const request = {
  timeout: 'Request has timed out.'
}

export default {
  any,
  fallback,
  http,
  request
}
